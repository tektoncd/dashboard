name: Rerun Failed Actions

permissions:
  contents: read

on:
  repository_dispatch:
    types: [retest-command]

jobs:
  retest:
    name: Rerun Failed Actions
    uses: tektoncd/plumbing/.github/workflows/_chatops_retest.yml@caef64c6adc701b46ec6151dc7c7a9282170f7b2 # main
    secrets:
      CHATOPS_TOKEN: ${{ secrets.CHATOPS_TOKEN }}
