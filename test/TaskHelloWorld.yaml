apiVersion: tekton.dev/v1alpha1
kind: Task
metadata:
  name: echo-hello-world-1
spec:
  steps:
    - name: echo
      image: ubuntu
      command:
        - echo
      args:
        - "hello world :) "