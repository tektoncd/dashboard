apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: hello-pipeline
spec:
  tasks:
  - name: do-hello-world
    taskRef:
      name: echo-hello-world
  - name: do-hello-world-1
    taskRef:
      name: echo-hello-world-1